# Newbiz 기능정의서 작성규칙

> v01 · 2026-08-18
> 선행 문서: `EAP_기능정의서_작성규칙_v04_20260813.md` (BizCare 13열 컨벤션)
> 본 문서는 위 문서를 **에임메드 LEC v09 양식(19열)** 기준으로 전환·취합한 것이다. 승계·폐기 내역은 §14 참조.

---

## 0. 문서 위상

| 문서 | 관리 대상 |
|---|---|
| WBS | 범위 · 일정 · 담당자 · 공수 (범위의 최종 기준) |
| **기능정의서** (본 규칙) | 화면별 컴포넌트 상세 · 정책 · 속성 |
| 화면설계서 작성규칙 | 피그마용 HTML · 프로토타입 작성 기준 |

- **화면목록 문서는 폐기.** 범위는 WBS, 화면 상세는 기능정의서에서 단일 관리한다.
- 기능정의서와 화면설계서는 **Component ID로 상호 추적**한다. 불일치 발견 시 먼저 플래그하고 확인을 받은 뒤 수정한다.

---

## 1. 컬럼 구조 (19열 고정 · LEC v09)

| 열 | 컬럼명 | 작성 기준 |
|---|---|---|
| A | no | 문서 전체 통산 순번. 행 추가·삭제 시 전 시트 재부여 |
| B | Project ID | `EAP-FE`(프론트엔드) / `EAP-BO`(백오피스) |
| C | 분류 | `Mobile web` / `Desktop web` / `App` / `Admin(desktop) web` — 복수 시 ` / ` 구분 |
| D | 1Depth | 화면 분류 (공통 / 홈 / 예약 …) |
| E | 2Depth | 화면 그룹 |
| F | 3Depth | 영역 |
| G | 4Depth | 세부 요소 |
| H | Screen Name | 한글 화면명 |
| I | Screen ID | 영문 PascalCase (§2) |
| J | Component Name | 컴포넌트 사전 등재명 (§3) |
| K | Component Short | 컴포넌트 사전 약어 |
| L | Component No | 동일 컴포넌트 내 일련번호 (1부터) |
| M | Component ID | `{Screen ID}-{Short}-{No}` (§2) |
| N | Components | `###` 제목 + 번호 구성요소 (§4) |
| O | Property | `#### 속성` 마크다운 (§5) |
| P | 비고(컬럼) | 검토 항목 · 미확정 표기 (§8) |
| Q | 최종수정일시 | `YYYY-MM-DD`. **수정한 행만** 갱신 |
| R | 수정셀 | 해당 행에서 변경한 컬럼명 나열 |
| S | 담당자 | 작성자명 |

**Depth 작성 기준**
- Depth는 **화면명까지만** 사용하고, 필요 없는 하위 Depth는 비운다.
- 이전 규칙의 5Depth는 폐기(19열 양식에 4Depth까지만 존재).

---

## 2. 명명 규칙

### Screen ID
- 영문 **PascalCase**. 예) `Main`, `Reservation`, `HospitalList`, `MobileLayout`, `PcLayout`, `PlatformSwitch`
- **PC 전용 Screen ID는 만들지 않는다.** 모바일과 공용하고 레이아웃 컴포넌트(`PcLayout-*`)로만 구분한다.
- 레이아웃 ID는 대상 디바이스를 이름에 드러낸다: 모바일 전용은 `MobileLayout`, PC 전용은 `PcLayout`. `CommonLayout` 같은 모호한 이름은 사용하지 않는다.

### Component ID
```
{Screen ID}-{Component Short}-{Component No}
예) Main-sldr-1 · PcLayout-bnr-2 · PlatformSwitch-sw-1
```
- 하이픈 3분절 고정. 붙여쓰기(`Main-sldr1`)·2분절 형식은 사용하지 않는다.
- 화면 변형(팝업·상태 분기)은 **별도 화면이 아니라 컴포넌트 행으로 통합**한다.

### 폐기된 화면 ID 체계
- 구 체계 `eap-{분류}-{2자리}`(예: `eap-main-01`)는 폐기. 기존 산출물에 남아 있으면 위 체계로 치환한다.

---

## 3. 컴포넌트 사전

- Component Name·Short는 **컴포넌트 사전 시트에 등재된 값만** 사용한다.
- 사전 미등재 컴포넌트가 필요하면 ① 근접한 사전 항목으로 치환하거나 ② 사전에 신규 등재 후 사용한다. 임의 명명 금지.
- 사전의 정의와 실제 용도가 어긋나더라도 **에임메드 관행이 우선**한다.
  - 예) 배너 캐러셀은 사전 정의상 Carousel이 가깝지만, 관행에 따라 `Sliders / sldr`로 표기한다.
- 약어가 단어 그대로인 예외가 있다: `Title / Title` → `Main-Title-1`

---

## 4. Components(N열) 작성 규칙

```
### 컴포넌트 제목
1. 구성요소
2. 구성요소
3. 구성요소
```

- 첫 줄은 반드시 `###` 제목.
- 번호는 최상위 `1.` `2.` `3.`, 하위는 `1-1.` `1-2.`
- **Property의 번호와 1:1 대응**시킨다.
- UI 요소 종류는 Component Name 열이 담당하므로, 이전 규칙의 `[대괄호]` 표기(`[CTA 버튼]`, `[블럭]` 등)는 기능정의서에서 사용하지 않는다. 단 **화면설계서 description에서는 계속 사용**한다.

---

## 5. Property(O열) 작성 규칙

```
#### 속성
- 정책·동작·제약
- 디바이스별 차이
```

- 헤딩은 **`#### 속성`으로 통일**. `#### 정책`·`### 속성` 등 혼용 금지.
- 동작은 **동작 → 결과** 형식으로 기술한다.
- 분기는 번호 + 조건으로 명시한다.
  ```
  1. 미신청 → "미신청" 뱃지 + 전체폭 "검진 예약하기" 버튼
  2. 신청완료 / 예약확정 → 상태 뱃지 + 병원·일시 + 작은 "예약 조회" 버튼
  3. 수검완료 → 카드 비노출
  ```
- 공통 컴포넌트는 **1행으로 쓰고** Property에 디바이스 차이를 메모한다.
- 관련 컴포넌트는 Component ID로 상호 참조한다. 예) `홈에서는 FAB 대신 도움말 리스트(Main-list-1)로 대체`

---

## 6. 시트 구성

- **WBS 단계 기준으로 시트를 분리**한다. 예) `2.1_공통` `2.2_홈(메인)` `2.3_건강검진예약` `2.7_관리자`
- 시트명 괄호와 탭 색상으로 진행 상태를 표시한다.

| 상태 | 시트명 표기 | 탭 색상 |
|---|---|---|
| 진행중 | `(진행중)` | 파랑 |
| 예정 | `(예정)` | 회색 |
| 완료 | `(완료)` | — |
| 보류 | `(보류)` | — |

- 부속 시트: `00_표지·변경이력` / `명명 규칙` / `컴포넌트 사전`
- 기획 순서: 공통 › 홈(메인) › 예약 › 결과 › 부가서비스 › 로그인·마이 › 서비스도움말 › 관리자

---

## 7. 시트 서식

### 텍스트 줄바꿈
| 대상 | 설정 | 이유 |
|---|---|---|
| 표지 시트 B열·D열 | **오버플로우**(자동 줄바꿈 해제) | 한 줄로 읽히도록. 행 높이 고정도 함께 해제 |
| 본문 시트 Components(N)·Property(O) | **자동 줄바꿈 유지** | 번호 목록·마크다운 헤딩이 여러 줄 구조 |
| 그 외 열 | 오버플로우 | 값이 짧아 줄바꿈 불필요 |

### 틀 고정
- **전 시트 틀 고정(행·열 고정) 사용하지 않는다.**

### 기본 서식
- 폰트 Arial · 본문 9pt / 헤더 10pt Bold
- 헤더 배경 `1F3864` · 흰색 글자 · 행 높이 30
- 본문 세로 정렬 top

---

## 8. 비고(P열) 규칙

### 표기 패턴
| 태그 | 의미 |
|---|---|
| `[TBD]` | 정책 미확정 |
| `[결정필요]` | 의사결정 대기 |
| `[개발확인]` | 개발팀 확인 필요 |
| `[검수]` | 법무·의료 검수 필요 |
| `[선행]` | 타 과제 선행 의존 |

### 색상 규칙 (openpyxl 생성 시)
| 색상 | HEX | 사용 조건 |
|---|---|---|
| 노랑 | `FFFF99` | 검토 필요 / 미확정 (`[TBD]` `[결정필요]` `확인 필요` `재검토`) |
| 주황 | `FFC000` | 외부 연동 (공단 API · 지도 API · ASP 연동 · 웰잇고 SSO) |
| 하늘 | `BDD7EE` | 교차 참조 / 타 화면 연계 |
| 흰색 | `FFFFFF` | 변경 없음 (기본) |

---

## 9. 수정 이력 기재

- **수정한 행만** Q열(최종수정일시)을 당일로 갱신한다. 미수정 행은 이전 날짜를 유지한다.
- R열(수정셀)에 변경한 컬럼명을 나열한다. 예) `Component ID·Property·비고(컬럼)`
- 신규 행은 R열에 `신규 행`으로 표기한다.
- 표지 `■ 변경이력`에 버전 단위 요약을 남긴다.

---

## 10. 버전 · 파일명

- 파일명: `{프로젝트명}_{기능명}_{파일명}_{버전}_{YYYYMMDD}`
  - 예) `Newbiz_공통UI및홈화면_기능정의서_v0.8_20260818`
- **버전은 명시적 승인 없이 올리지 않는다.** 날짜는 항상 작업 당일로 갱신한다.
- 기능정의서와 화면설계서는 **동일 버전 번호로 짝을 맞춘다.**
- 파일명의 기능명이 실제 문서 범위보다 좁을 경우, 표지에 실제 범위를 명시한다.

---

## 11. 화면설계서와의 정합

- 화면설계서 description의 컴포넌트 ID 칩은 기능정의서 Component ID와 **완전히 일치**해야 한다.
- 기능정의서에 정의된 컴포넌트는 화면설계서에 대응 요소·번호 뱃지가 있어야 한다. 화면이 없는 공통 상태(토스트·로딩)는 별도 상태 화면 섹션으로 그린다.
- 한쪽만 변경한 경우 나머지 문서의 반영 필요 여부를 함께 보고한다.

---

## 12. 검증 절차

산출 전 아래를 확인하고, 통과 후 파일을 제시한다.

| # | 항목 | 기준 |
|---|---|---|
| 1 | Component ID 패턴 | 전 행이 `{Screen ID}-{Short}-{No}`와 일치 · 중복 없음 |
| 2 | no 연번 | 문서 전체 통산 순번이 끊기지 않음 |
| 3 | 마크다운 헤딩 | Components `###` 시작 / Property `#### 속성` 시작 |
| 4 | Depth | 사용하지 않는 Depth 열이 비어 있음 |
| 5 | 컴포넌트 사전 | Name·Short가 사전 등재값과 일치 |
| 6 | 이모지 | 검출 0건 (`※` 등 일반 기호는 허용, `★` 등 특수문자는 지양) |
| 7 | 서식 | 폰트·헤더 색·열 너비 유지 · 틀 고정 없음 |
| 8 | 화면설계서 정합 | Component ID 상호 대조 |

---

## 13. 작업 지시 템플릿

```
[작업 유형] 신규 생성 / 업데이트 / 동기화
[기준 파일] (파일명)
[변경 범위] 예: 2.2 홈 시트에 콘텐츠 영역 컴포넌트 추가
[제약] 규칙 문서 준수 / 화면설계서 정합 유지
[버전] 유지 / 올리기
[날짜] 오늘 날짜 적용
```

---

## 14. 선행 문서(EAP v04) 승계·폐기 내역

### 승계
- 표시내용 ↔ 내용 **번호 1:1 동기화** 원칙 → §4·§5
- **동작 → 결과** 기술 방식, 분기의 번호+조건 표기 → §5
- **비고 색상 규칙**(노랑·주황·하늘) → §8. 단 주황의 조건은 `SAKAK·에임스` → 현 프로젝트 외부 연동으로 갱신
- 진입 조건·전제 설명을 별도로 기술하는 방식 → Property 상단 서술로 승계

### 폐기
| 폐기 항목 | 대체 |
|---|---|
| 13열 컬럼 구조 | 19열 LEC v09 (§1) |
| 화면 ID `eap-{분류}-{2자리}` | Screen ID PascalCase + Component ID (§2) |
| 5Depth | 4Depth까지 (§1) |
| 화면 종류 열(Page/블럭/필터/버튼) | Component Name + 컴포넌트 사전 (§3) |
| 표시내용의 `[대괄호]` 표기 | Component Name 열로 대체. 화면설계서 description에서만 유지 (§4) |

---

## 15. 버전 이력

| 버전 | 날짜 | 변경 내용 |
|---|---|---|
| v04 | 2026-08-13 | (선행) BizCare 13열 컨벤션 |
| v01 | 2026-08-18 | Newbiz 기준 신규 작성. LEC v09 19열 전환, 명명 규칙(PascalCase·하이픈 3분절)·컴포넌트 사전·시트 구성/서식·검증 절차 신설. EAP v04 승계·폐기 내역 명시 |
