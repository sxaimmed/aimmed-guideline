# 1. 기본적인 설계 디스크립션은 상위 정책을 따른다.
<!-- 
히스토리 v03 20260617 KPI P열까지 추가
-->
## 화면 구성시 제한
1. **기능 요구사항 외에 추가/변경 시 반드시 사용자의 승인 과정을 거친다.**

# 1. 기능정의서.md (MD파일 분리예정)
## 기능정의서 설명 **(추후 파일 분리 예정)**

#### 스프레드시트 또는 엑셀파일 화면설계서를 분석에서 페이지와 UI를 생성하는 규칙이 포함된 마크다운 문서

### 기능정의서 기본 사용 규칙

1. 스프레드 시트의 행과 열을 잘 사용한다.
2. 행(Row)은 각각 항목을 기록한다.
3. 열(Colulm)은 반복되는 요소를 정리한다.
4. C열 분류는 현재 화면 또는 기능의 그룹등을 정의한다.
5. D열~G열은 메뉴의 상위하위 체계를 정의한다. 메뉴는 트리구조로 되어있다. 
6. H열 5Depth은 페이지 하의의 블록, 기능, 모듈 명 등을 정의한다.


### 컬럼명

1. A열 : 번호,
2. B열 : Project ID
3. C열 : 분류, Mobile web, Desktop web, App, Admin(desktop) web
4. D열 : 1 Depth 메뉴/화면명
5. E열 : 2 Depth 메뉴/화면명
6. F열 : 3 Depth 메뉴/화면명
7. G열 : 4 Depth 메뉴/화면명
8. H열 : 화면명(Screen Name)
9. I열 : 화면명(Screen ID)
10. J열 : Component Name(컴포넌트 타입), Screen, Popup, Layer Popup, Bottom Sheet, Section, Component 등
11. K열 : Component Snort(컴포넌트 ID를 짧게 변형)
12. L열 : Conponent no (컴포넌트 순번, Component ID가 중복되면 끝자리에 자동순번 추가  1,2,3,4)
13. M열 : Component ID, CSS 클래스에 추가, 컴포넌트 단위 업데이트시 사용, Component ID가 중복되면 끝자리에 자동순번 추가  1,2,3,4)
14. **N열 : 표시내용 컴포넌트(Component)**, H3 사용, 순서있는목록 OL 적용   
15. **L열  : Property(컴포넌트 속성) N열에 대한 속성 및  추가 기능 정의**, H4 사용, 같은 행의 N열 OL항목에 연결된다.
16. P열 : 비고, 해당 컴포넌트의 비고, 사전처리 조건 등
17. Q열 변경일시 : 현재 행에서 수정, 업데이트한 날짜와 시간
18. R열 변경컬럼 : 현재 행에서 변경된 컬럼 목록
19. S열 담당자 : 수정한 담당자 이메일

###  기능정의서 메뉴트리 구조
1. 기능정의서 기능정의 탭에서 1Depth~4Depth 까지 피벗 형태의 트리구조로 되어있다.
2. 화면 종류는 Page, 블록, 팝업, 레이어팝업, 빈페이지 등이 있다.
3. ** 화면 종류에서 블록, 버튼, 텍스트, 콘텐츠는 메뉴 구조에서 제외한다.**
4. 화면 Depth, 화면명, 화면종류를 구분하여 메뉴트리를 순서없는 목록(UL)형태로 마크업한다.

### 기능정의서의 구조
기능정의서에서는 Screen, Section, Component, Element 총 4단계를 사용하되 **Component, Element를 합쳐 3단계로 사용을 권장한다.** Component가 여러개의 Element로 구성된 경우에는 4단계까지 허용한다.
1. Screen(화면) : Page
2. Section(영역) : Layout
3. Component(컴포넌트) : 1개 또는 여러개의 Element로 되어 있다.
    > 1. 목적 : 이 컴포넌트가 해결하는 사용자 과제
    > 2. 입력값 : 사용자가 입력하거나 외부에서 받는 데이터, 기본값, 필수여부
    > 3. 노출조건 : 언제 보이고 언제 숨는지
    > 4. 상태 : default, loading, empty, error, disabled, selected 등
    > 5. 이벤트 : tap, click, change, submit, swipe 등
    > 6. 결과 : 화면 이동, API 호출, 토스트 표시, 값 저장 등
4. Element(요소) : text, image, form, Placeholder, Label, Icon, Validation Message 등
    > 1. Property(속성)
    > 2. State(상태)
    > 3. Event(이벤트)


## 변경/확인 필요 사항 (사용자 승인 필요)
 
> 상위 정책상 기능 요구사항 외 추가/변경은 승인이 필요합니다. 아래는 원문(폼요소v01.md)에서 발견한 정리 사항입니다.
 
1. **2.1 제목 수정 제안**: 원문 2.1의 제목이 "패스워드 입력 필드"였으나 내용은 텍스트 입력 필드이며, 2.2가 "텍스트 입력폼의 정책을 따른다"고 참조하므로 **"텍스트 입력 필드"** 로 정정했습니다. (확인 요망)
2. **번호 중복 정리**: 원문 말미의 "2.3 로그인 버튼"이 2.3(날짜)과 중복되어 **2.12**로 재번호했습니다.
3. **원문 미기재 항목**: 각 컴포넌트의 `목적 / 노출조건 / 일부 이벤트`는 원문에 명시가 없어 "원문 미기재" 또는 항목명 기반으로 보수적으로만 기술했습니다. 실제 기능 정의가 필요하면 별도 승인 후 보강하겠습니다.
4. **원문 오타 보존**: 2.1-6 "저장하지 않거"(→않고), 2.1-2 "20글"(→20글자) 등은 의미 전달에 지장이 없어 본문에서는 정정 표기했으나, 기능 변경은 아닙니다.



